#!/usr/bin/env bash
# Make sure to test your script with several users by providing them as argument.
# script should execute the whoami command with the user supplied as an input.

sudo -u "$1" whoami
